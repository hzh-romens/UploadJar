package com.ifeng.uploadassitant.media.listener;


/**
 *  when original is enabled , callback immediately when user check or uncheck original.
 */
public interface OnCheckedListener {
    void onCheck(boolean isChecked);
}
